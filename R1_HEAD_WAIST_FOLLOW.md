# R1-A7 头腰联动

在已有 `teleop/teleop_hand_and_arm.py --arm R1_A7 ...` 启动命令末尾加入：

```bash
--waist-follow --arm-diagnostic-dir /tmp/r1-head-waist-diagnostics
```

保留原有图像地址、网卡等参数；O6 已迁移到 `--linker-o6-method vector|position|dexpilot`，不再接受旧标定文件参数。未加 `--waist-follow` 时不启用腰跟随。此参数不能用于其他机器人、`--motion` 或手部隔离 dry-run。旧的 `visionpro_r1_a7_o6_sim.py` JSON 预览链路未接入此功能。

首次验收应使用匹配 R1-A7、支持腰关节控制的 DDS 仿真，主程序加 `--sim`，并使用仿真的网卡和图像地址；此模式使用 DDS domain 1，不调用真机模式切换。不要把 G1 场景当成 R1-A7 来运行。现有 Linker O6 实体控制参数不能与 `--sim` 混用；双臂/头腰仿真时先不传 `--ee`。

## 行为

- 小幅、短暂转头只控制头部。头部相对当前腰角的偏航超过 20°，同方向持续 0.4 秒后腰开始跟随；剩余偏航小于 5°后平滑停止跟进。这是意图判定，不是腰部角度限幅。
- **没有额外的 ±30° 活动范围限制**。最终腰目标仅按当前 URDF 的 `±2.618 rad`（约 ±150°）关节边界约束；这不是对实机可安全达到该范围的保证。
- 腰目标平滑时间常数 0.8 秒，速度不超过 0.35 rad/s（约 20°/s），分配器加速度不超过 0.5 rad/s²。底层另外限制目标相对实测腰角的跟踪误差，避免电机未跟上时命令累积。
- IK 仍锁定启动参考腰角。双手完整位姿按 `Rz(waist_reference - waist_actual)` 补偿，不使用腰目标角代替反馈角。头部按实际腰角求剩余视线方向，并遵守头关节模型边界。
- XR 断流超过 `--tracking-timeout`（默认 250 ms）后不再发送新的跟随目标，腰保持反馈角；恢复后重锚腰目标、清除速度并重新等待持续转头。发布线程还有独立 250 ms 腰目标/反馈看门狗。
- R1-A7 的 `--input-mode hand` 按左右手分别判断超时：一侧超时后保留最后已提交的根坐标系腕部目标，另一臂继续跟随；双手都超时才暂停双臂更新。重新识别的一侧先保持一帧，随后继续使用启动时的固定参考，不重新建立相对原点。遮挡期间的位移和转动可能被追赶；恢复追踪不代表保证无跳变。连接不会因单手遮挡而主动断开。
- 任一手超时时，头和腰暂停跟随。单手继续控制期间，腕部目标仍按实际腰角补偿；双手恢复后再重新等待持续转头。O6 超时侧沿用最后的手指指令，另一侧仍可更新。保持的是目标，不保证实际末端严格不动，仍受 IK 误差和跟踪性能影响。
- 启动依然要求双手有效。上述独立跟随适用于主程序的 R1-A7 真机和 `--sim --input-mode hand`（仿真无需开启腰跟随），不改变 controller 模式、手部隔离 dry-run 或旧 JSON 预览链路。终端 `[R1 HAND TRACKING] left/right` 显示独立暂停/恢复，诊断 JSONL 的 `hand_tracking` 记录各侧新鲜度与数据年龄。
- `r` 只需在启动时按一次：等待有效追踪后回正头腰、加载 IK；随后等待双手连续有效至少 0.35 秒且有至少 5 个新样本，再自动采集参考并开始跟随。回零或加载期间短暂丢失手部追踪不会直接退出，也不需要再按 `r`，不重复回零。等待阶段双臂/头腰持续使能保位，O6 尚未使能；不是断电或无力状态。机器人反馈超时或腰参考漂移仍会停止。`q` 退出并停止控制发布，但不是实体急停，原约 3 秒回零期间仍可能延迟响应。

## 验收与日志

依次验证：短暂左右看、持续左右转头、超过 30°、转腰时固定双手目标、运动中反向、遮挡手或断开 Vision Pro、恢复连接。先在仿真中检查，再在有急停、清空工作区且确认线缆余量的条件下低速验收真机。

终端每秒输出 `[R1 HEAD/WAIST]`：总 yaw、实际腰角、腰目标、头 yaw 和跟随状态。JSONL 诊断包含腰角、补偿前后双手目标、FK 位姿及位置/旋转误差；FK 已变换回实际机器人根坐标系。启用 `--record` 时，`body.qpos` 的状态/动作顺序为 `[waist_yaw, head_pitch, head_yaw]`。

本次提供的是离线数学和模拟控制器测试，不代表已经通过 Isaac 动态或真机验收。现有 IK 的末端误差是软代价：目标不可达时，即使求解器未报错也可能有偏差；它也不提供完整的自碰撞、环境碰撞或线缆保护。大腰角下必须查看实际/求解末端误差，不能仅凭 `following=True` 判断协调正确。低速腰运动的动态力矩影响尚待验收。

## 本轮验证（2026-09-07）

Ubuntu 现有 `.venv-xr` 环境中的 74 项相关测试全部通过：头腰分配 17 项、腰控制器 17 项、主循环集成 9 项、既有启动 16 项、手部 dry-run 隔离 5 项、O6 控制器 10 项。完整 URDF 的 Pinocchio FK 测试覆盖非零参考腰角、实际腰角及随机双臂构型；控制器和主循环测试使用 mock，不发送 DDS 指令。语法编译及 `--help` 参数检查通过。

Mac 本地全量测试曾因缺少 O6 URDF、相邻仿真工程文件和 CasADi 等依赖未通过；上述 74 项在 Ubuntu 的实际开发环境中复验通过，不代表仓库所有测试都已通过。

同步位置：`/home/hnh/unitree_r1_dev/xr_teleoperate`。改动前的主程序、控制器和启动测试备份在 `/home/hnh/unitree_r1_dev/waist-follow-backup.3dqVc0`。未覆盖远端既有 `robot_arm_ik.py`，未运行遥操或真机控制。

## 双手独立跟随验证（2026-09-07）

Ubuntu `.venv-xr` 全套 191 项测试通过，包含左右手独立超时、单侧持续跟随、恢复时平移/旋转重新对齐、IK 期间数据过期不提交、实际腰角补偿、O6 单侧沿用最后指令、录制数值及逐侧诊断。`--help` 和修改文件语法检查通过。这些是离线数学/模拟控制器测试，尚未进行本功能的 Vision Pro、Isaac 动态或真机验收；未重启遥操进程，未发送真机动作命令。

原启动参数无需增加开关；重新启动主程序才能加载修改。测试先双手有效启动，再遮挡右手并移动左手、交换左右、重新识别并检查没有追赶遮挡期间的动作、最后双手遮挡。Ubuntu 修改前备份：`/home/hnh/unitree_r1_dev/independent-hands-backup.E7uLD5/source-before.tar.gz`。
